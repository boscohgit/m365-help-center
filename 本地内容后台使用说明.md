# M365 本地内容后台使用说明

Mac 和 Windows 使用同一套内容及 GitHub 仓库：

- Mac 双击 `启动本地内容后台.command`
- Windows 双击 `启动本地内容后台-Windows.cmd`

Windows 第一次使用和跨电脑同步方法见 `Windows使用说明.md`。

## 启动

1. 双击项目根目录里的 `启动本地内容后台.command`。
2. 保持弹出的终端窗口开启。
3. 浏览器会自动打开 `http://127.0.0.1:15986`。

如果浏览器没有自动打开，请手动复制上面的地址到浏览器。

## 新建一篇 SOP

1. 点击左上角“+ 新建 SOP”。
2. 填写标题、分类和网址标识。网址标识只能使用小写英文字母、数字和连字符，创建后不能修改。
3. 点击“创建并开始编辑”。
4. 后台会生成一个包含初始章节和第一步的空白模板，继续补充文字和截图即可。

新建的 SOP 会自动进入网站列表、独立网页和搜索索引，不需要手动修改代码。

## 整理首页分类和顺序

1. 点击左侧“整理首页分类与顺序”。
2. 在同一个类目中拖动 SOP 卡片，即可调整这些文档在首页的先后顺序。
3. 把 SOP 卡片拖到其他类目中，即可修改它在首页的归类。
4. 按住类目标题旁的 `⋮⋮` 拖动整个类目，即可调整首页类目的显示顺序。
5. 点击“保存首页整理”，右侧预览会自动切换到首页。
6. 检查无误后，再点击顶部“发布到 GitHub”。

## 编辑和调整顺序

1. 在左侧选择一篇指引。
2. 直接修改标题、说明、步骤或图片说明。
3. 按住 `⋮⋮` 拖动章节、步骤或图片，网页会按照后台中的顺序显示。
4. 点击“保存草稿”后，修改会写入本地项目。
5. 点击“刷新预览”，在右侧检查实际网页效果。

## 添加和脱敏图片

1. 在对应步骤下面点击“添加图片”，也可以把图片文件拖到对应的图片区域。
2. 在弹出的脱敏窗口里，用鼠标框住姓名、邮箱、电话号码、密码、验证码、二维码等敏感信息。
3. 点击“完成并添加图片”。
4. 填好图片说明，然后保存草稿并刷新预览。

已有图片可以点击“重新脱敏”。后台会生成一张新的脱敏图片，旧文件不会自动删除。

## 发布到 GitHub Pages

1. 确认右侧预览无误。
2. 点击“发布到 GitHub”。
3. 填写一句修改说明并确认。
4. 后台会自动构建网站、提交内容并推送到 GitHub。
5. GitHub Pages 通常需要稍等一会儿才会显示新版本。

发布按钮只允许提交后台管理的指引内容和截图。如果项目里还有其他代码改动，后台会停止发布，避免误提交。

## 关闭

关闭启动后台时弹出的终端窗口即可。关闭前请确认页面显示“草稿已保存”或“已发布到 GitHub”。
